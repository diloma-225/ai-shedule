name: Trigger Coze Workflow

on:
  workflow_dispatch:    # permet de déclencher manuellement le workflow
  # schedule:
  #   - cron: "0 8 * * *"  # optionnel : pour exécuter automatiquement tous les jours à 08h UTC

jobs:
  run-coze:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coze via curl
        run: |
          curl -X POST "https://api.coze.com/v1/workflow/run" \
            -H "Authorization: Bearer cztei_05Ym6efksV8dFSYFxjd10xpoec7M7EpPu7JnSb3afB4ntuNflG4bj9Hrkac9LEgWF" \
            -H "Content-Type: application/json" \
            -d "{\"workflow_id\": \"7587434991573336069\", \"parameters\": {\"input\": \"Abidjan\"}}"
